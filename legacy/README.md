# 历史代码

原 `code/` 和 `pangu.yml` 原样移到此处，仅用于追溯，不属于新的运行入口。

- 旧推理固定循环 6 小时模型，不符合原文分层时间聚合；忽略不足 6 小时的余量。
- 旧数据转换不按实际坐标重排，不能保证气压层、经纬度和起报时刻正确。
- 旧输出把气压层写成 1…13，时间按固定间隔猜测。
- 旧追踪包含区域硬编码、经度边界处理不足，并可能使用未来有效时刻的观测位置初始化追踪。
- 部分旧脚本在导入时执行，不应从新包中导入；其中的旧机器路径和依赖清单不再适用。

新运行入口是仓库根目录下的 `python -m pangu_weather`。
